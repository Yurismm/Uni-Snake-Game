#pragma once
class AISnake {
	// to check a collision you can use a pointer
	// to the other snake.

	//

	//
public:
	int length;
	int colour;
	bool followingSnake;
	// something for player position
	bool hasCollided;
};
